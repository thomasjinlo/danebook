
  <nav class="navbar navbar-container">
    <div class="container">

        <!-- Header Wrapper -->
      <div class="navbar-header">

       <!-- Collapses search and profile under toggle button -->
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

       <!-- Brand Logo -->
        <%= link_to current_user, class: "navbar-brand"  do %>
          <img src="/images/danish_flag.gif" class="logo">
        <% end %>
        <%= link_to 'Danebook', current_user, class: "navbar-brand" %>
      </div>

     <!-- Input form & profile set to collapse -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

       <!-- Search Bar -->
        <% if signed_in_user? %>
          <%= form_tag user_searches_path(current_user), method: :get, class: "navbar-form navbar-left" do %>
            <div class="form-group">
              <%= text_field_tag(:search_name, "", class: "form-control", placeholder: "Search for Users") %>
            </div>
          <% end %>
        <% end %>

        <!-- Link to profile -->
        <ul class="nav navbar-nav navbar-right">
          <% if signed_in_user? %>
            <li class="nav-links"><%= link_to current_user.first_name, current_user, class: "nav-links" %></li>
            <li><%= link_to "Logout", logout_path, class: "nav-links", method: :delete %></li>
          <% else %>
            <li><%= link_to "Sign In", new_user_path %></li>
          <% end %>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
