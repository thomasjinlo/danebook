<div class="row recent-activity-row">
  <div class="col-xs-3">
    <% if post.author.avatar.nil? %>
      <img src="/images/icon_photo_small.png" class="img-responsive thumbnail">
    <% else %>
      <%= image_tag post.author.avatar.image.url(:thumb), class: "img-responsive", alt: "missing photo" %>
    <% end %>
  </div>

  <div class="col-xs-9">
    <%= link_to post.author.full_name, post.author %>
    <span>
      Said on <%= post.created_at.strftime("%m/%d/%Y") %>
    </span>

    <div class="newsfeed-posts">
      <%= post.body %>
    </div>
  </div>
</div>
